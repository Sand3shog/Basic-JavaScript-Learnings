// ? array destructre
// object ma key hunxa, yesma key hudaina

const fullName = ['Sandesh', 'Mahrjan'];
const [firstName, lastName] = fullName;          // we can assign variable ourselves here as object has variable already assigned but array dont have, so
console.log(firstName);


