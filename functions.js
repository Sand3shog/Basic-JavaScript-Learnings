const addNumber = (x, y) =>{
    const sum = x + y;
    return sum;
};

const multiplyNumber = (x, y) =>{
    const multiply = x * y;
    return multiply;
}
    // const PI = 3.14;              //we can also export PI variable
export { addNumber, multiplyNumber };

